import { render, screen } from '@testing-library/react';
import ResrtrurantCard from '../ResrtrurantCard';

it('should resturant card component with props data', () => {
    render(<ResrtrurantCard  resData=""/>)
    // screen
})